<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MaleficBoss — Developer & Writer</title>
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Sora:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #0a0a0a;
  --fg: #f0ece4;
  --accent: #c8ff00;
  --accent2: #00e5a0;
  --muted: #6b6b6b;
  --card-bg: #141414;
  --card-border: #222;
  --mono: 'Space Mono', monospace;
  --sans: 'Sora', sans-serif;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  background: var(--bg);
  color: var(--fg);
  font-family: var(--sans);
  overflow-x: hidden;
  line-height: 1.6;
}

::selection { background: var(--accent); color: var(--bg); }

.noise {
  position: fixed; top: 0; left: 0; width: 100%; height: 100%;
  background: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
  pointer-events: none; z-index: 9999;
}

nav {
  position: fixed; top: 0; left: 0; right: 0; z-index: 100;
  padding: 1.5rem 3rem;
  display: flex; justify-content: space-between; align-items: center;
  backdrop-filter: blur(20px);
  background: rgba(10,10,10,0.8);
  border-bottom: 1px solid var(--card-border);
}

nav .logo {
  font-family: var(--mono);
  font-weight: 700;
  font-size: 1.1rem;
  letter-spacing: -0.5px;
}

nav .logo span { color: var(--accent); }

nav ul { display: flex; gap: 2rem; list-style: none; }

nav a {
  color: var(--muted);
  text-decoration: none;
  font-size: 0.85rem;
  font-weight: 500;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  transition: color 0.3s;
}

nav a:hover { color: var(--accent); }

.hero {
  min-height: 100vh;
  display: flex; flex-direction: column; justify-content: center;
  padding: 8rem 3rem 4rem;
  max-width: 1200px; margin: 0 auto;
  position: relative;
}

.hero-tag {
  font-family: var(--mono);
  font-size: 0.8rem;
  color: var(--accent);
  letter-spacing: 2px;
  text-transform: uppercase;
  margin-bottom: 1.5rem;
  opacity: 0;
  animation: fadeUp 0.6s ease forwards 0.2s;
}

.hero h1 {
  font-size: clamp(2.8rem, 7vw, 5.5rem);
  font-weight: 700;
  line-height: 1.05;
  letter-spacing: -2px;
  margin-bottom: 1.5rem;
  opacity: 0;
  animation: fadeUp 0.6s ease forwards 0.4s;
}

.hero h1 em {
  font-style: normal;
  background: linear-gradient(135deg, var(--accent), var(--accent2));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.hero-sub {
  font-size: 1.15rem;
  color: var(--muted);
  max-width: 560px;
  line-height: 1.8;
  margin-bottom: 2.5rem;
  opacity: 0;
  animation: fadeUp 0.6s ease forwards 0.6s;
}

.hero-cta {
  display: flex; gap: 1rem; flex-wrap: wrap;
  opacity: 0;
  animation: fadeUp 0.6s ease forwards 0.8s;
}

.btn {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 0.85rem 2rem;
  border-radius: 50px;
  font-family: var(--sans);
  font-size: 0.9rem;
  font-weight: 600;
  text-decoration: none;
  transition: all 0.3s;
  cursor: pointer;
  border: none;
}

.btn-primary {
  background: var(--accent);
  color: var(--bg);
}

.btn-primary:hover {
  background: var(--accent2);
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(200,255,0,0.2);
}

.btn-ghost {
  background: transparent;
  color: var(--fg);
  border: 1px solid var(--card-border);
}

.btn-ghost:hover {
  border-color: var(--accent);
  color: var(--accent);
}

.hero-grid {
  position: absolute;
  right: -5%; top: 15%;
  width: 400px; height: 400px;
  background: radial-gradient(circle at 30% 40%, rgba(200,255,0,0.06) 0%, transparent 60%),
              radial-gradient(circle at 70% 60%, rgba(0,229,160,0.04) 0%, transparent 50%);
  border-radius: 50%;
  filter: blur(60px);
  pointer-events: none;
}

section { padding: 6rem 3rem; max-width: 1200px; margin: 0 auto; }

.section-label {
  font-family: var(--mono);
  font-size: 0.75rem;
  color: var(--accent);
  letter-spacing: 3px;
  text-transform: uppercase;
  margin-bottom: 1rem;
}

.section-title {
  font-size: clamp(1.8rem, 4vw, 2.8rem);
  font-weight: 700;
  letter-spacing: -1px;
  margin-bottom: 1rem;
}

.section-desc {
  color: var(--muted);
  font-size: 1rem;
  max-width: 600px;
  margin-bottom: 3rem;
}

.services-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1.5rem;
}

.service-card {
  background: var(--card-bg);
  border: 1px solid var(--card-border);
  border-radius: 16px;
  padding: 2rem;
  transition: all 0.4s;
  position: relative;
  overflow: hidden;
}

.service-card:hover {
  border-color: var(--accent);
  transform: translateY(-4px);
  box-shadow: 0 16px 40px rgba(0,0,0,0.4);
}

.service-card::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--accent), var(--accent2));
  opacity: 0;
  transition: opacity 0.3s;
}

.service-card:hover::before { opacity: 1; }

.service-icon {
  width: 48px; height: 48px;
  border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  font-size: 1.4rem;
  margin-bottom: 1.25rem;
  font-family: var(--mono);
  font-weight: 700;
}

.service-card:nth-child(1) .service-icon { background: rgba(200,255,0,0.1); color: var(--accent); }
.service-card:nth-child(2) .service-icon { background: rgba(0,229,160,0.1); color: var(--accent2); }
.service-card:nth-child(3) .service-icon { background: rgba(100,130,255,0.1); color: #6482ff; }
.service-card:nth-child(4) .service-icon { background: rgba(255,120,80,0.1); color: #ff7850; }

.service-card h3 {
  font-size: 1.15rem;
  font-weight: 600;
  margin-bottom: 0.75rem;
}

.service-card p {
  color: var(--muted);
  font-size: 0.9rem;
  line-height: 1.7;
}

.service-tag {
  display: inline-block;
  margin-top: 1rem;
  font-family: var(--mono);
  font-size: 0.7rem;
  color: var(--accent);
  letter-spacing: 1px;
  text-transform: uppercase;
  padding: 4px 10px;
  border: 1px solid rgba(200,255,0,0.2);
  border-radius: 20px;
}

.edge-section {
  margin-top: 2rem;
  background: var(--card-bg);
  border: 1px solid var(--card-border);
  border-radius: 20px;
  padding: 3rem;
  position: relative;
  overflow: hidden;
}

.edge-section::after {
  content: '';
  position: absolute;
  top: -50%; right: -20%;
  width: 400px; height: 400px;
  background: radial-gradient(circle, rgba(200,255,0,0.04), transparent 70%);
  pointer-events: none;
}

.edge-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
  margin-top: 2rem;
}

.edge-item {
  display: flex; gap: 1rem; align-items: flex-start;
}

.edge-num {
  font-family: var(--mono);
  font-size: 2rem;
  font-weight: 700;
  color: var(--accent);
  line-height: 1;
  min-width: 50px;
}

.edge-item h4 { font-size: 1rem; margin-bottom: 0.3rem; }
.edge-item p { color: var(--muted); font-size: 0.85rem; }

.process-steps {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1.5rem;
  counter-reset: step;
}

.step {
  padding: 1.5rem;
  border-left: 2px solid var(--card-border);
  counter-increment: step;
  transition: border-color 0.3s;
}

.step:hover { border-color: var(--accent); }

.step::before {
  content: "0" counter(step);
  font-family: var(--mono);
  font-size: 0.75rem;
  color: var(--accent);
  letter-spacing: 1px;
  display: block;
  margin-bottom: 0.75rem;
}

.step h4 { font-size: 1rem; margin-bottom: 0.5rem; }
.step p { color: var(--muted); font-size: 0.85rem; }

.pricing-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 1.5rem;
}

.price-card {
  background: var(--card-bg);
  border: 1px solid var(--card-border);
  border-radius: 16px;
  padding: 2rem;
  transition: all 0.3s;
}

.price-card.featured {
  border-color: var(--accent);
  position: relative;
}

.price-card.featured::before {
  content: 'MOST POPULAR';
  position: absolute;
  top: -10px; left: 1.5rem;
  font-family: var(--mono);
  font-size: 0.65rem;
  letter-spacing: 2px;
  background: var(--accent);
  color: var(--bg);
  padding: 3px 12px;
  border-radius: 20px;
  font-weight: 700;
}

.price-label {
  font-size: 0.85rem;
  color: var(--muted);
  margin-bottom: 0.5rem;
}

.price-amount {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 0.25rem;
}

.price-amount span {
  font-size: 0.9rem;
  font-weight: 400;
  color: var(--muted);
}

.price-desc {
  color: var(--muted);
  font-size: 0.85rem;
  margin-bottom: 1.5rem;
  padding-bottom: 1.5rem;
  border-bottom: 1px solid var(--card-border);
}

.price-features {
  list-style: none;
  font-size: 0.85rem;
}

.price-features li {
  padding: 0.4rem 0;
  color: var(--muted);
  display: flex; align-items: center; gap: 8px;
}

.price-features li::before {
  content: '~';
  color: var(--accent);
  font-family: var(--mono);
  font-weight: 700;
}

.cta-section {
  text-align: center;
  padding: 6rem 3rem;
  max-width: 700px;
  margin: 0 auto;
}

.cta-section h2 {
  font-size: clamp(2rem, 5vw, 3rem);
  font-weight: 700;
  letter-spacing: -1px;
  margin-bottom: 1rem;
}

.cta-section p {
  color: var(--muted);
  font-size: 1rem;
  margin-bottom: 2rem;
}

footer {
  border-top: 1px solid var(--card-border);
  padding: 2rem 3rem;
  display: flex; justify-content: space-between; align-items: center;
  max-width: 1200px; margin: 0 auto;
  font-size: 0.8rem;
  color: var(--muted);
}

footer a { color: var(--accent); text-decoration: none; }

@keyframes fadeUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@media (max-width: 768px) {
  nav { padding: 1rem 1.5rem; }
  nav ul { display: none; }
  .hero, section { padding-left: 1.5rem; padding-right: 1.5rem; }
  .edge-grid { grid-template-columns: 1fr; }
  .hero-grid { display: none; }
  footer { flex-direction: column; gap: 1rem; text-align: center; }
}
</style>
</head>
<body>

<div class="noise"></div>

<nav>
  <div class="logo">malefic<span>boss</span></div>
  <ul>
    <li><a href="#services">Services</a></li>
    <li><a href="#process">Process</a></li>
    <li><a href="#pricing">Pricing</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>

<section class="hero">
  <div class="hero-grid"></div>
  <div class="hero-tag">Developer / Writer / AI Workflow Specialist</div>
  <h1>I build fast.<br>I write sharp.<br>I ship <em>everything.</em></h1>
  <p class="hero-sub">Frontend developer and technical writer who uses AI-augmented workflows to deliver in days what takes others weeks. You get agency-quality output at freelancer prices.</p>
  <div class="hero-cta">
    <a href="#contact" class="btn btn-primary">Start a project &rarr;</a>
    <a href="#services" class="btn btn-ghost">See what I do</a>
  </div>
</section>

<section id="services">
  <div class="section-label">Services</div>
  <h2 class="section-title">What I deliver</h2>
  <p class="section-desc">Four things I do exceptionally well — each one powered by modern AI tools that let me move fast without cutting corners.</p>

  <div class="services-grid">
    <div class="service-card">
      <div class="service-icon">&lt;/&gt;</div>
      <h3>Landing pages & web builds</h3>
      <p>Responsive, fast-loading sites built with clean HTML/CSS and React. From single landing pages to multi-page sites. Deployed and live, not just mockups.</p>
      <span class="service-tag">3-7 day delivery</span>
    </div>
    <div class="service-card">
      <div class="service-icon">T</div>
      <h3>Technical writing & docs</h3>
      <p>API documentation, developer guides, README files, and technical tutorials. I read your codebase and write docs that developers actually want to read.</p>
      <span class="service-tag">Clear & structured</span>
    </div>
    <div class="service-card">
      <div class="service-icon">&para;</div>
      <h3>Content & blog writing</h3>
      <p>SEO-optimized blog posts, thought leadership articles, and newsletter content. Specializing in tech, fintech, and developer-focused audiences.</p>
      <span class="service-tag">Research-backed</span>
    </div>
    <div class="service-card">
      <div class="service-icon">&gt;_</div>
      <h3>AI workflow automation</h3>
      <p>I set up AI-powered workflows for your business — content pipelines, code generation, data processing, and task automation using Claude, APIs, and scripts.</p>
      <span class="service-tag">Save 10+ hrs/week</span>
    </div>
  </div>
</section>

<section id="projects">
  <div class="section-label">Projects</div>
  <h2 class="section-title">What I've built</h2>
  <p class="section-desc">Real projects, real code. From Solana bots to frontend challenges — here's a sample of my work.</p>

  <div class="services-grid">
    <div class="service-card">
      <div class="service-icon" style="background: rgba(100,130,255,0.1); color: #6482ff;">S</div>
      <h3>Kora Rent Reclaim Bot</h3>
      <p>TypeScript bot for Solana that automatically monitors and reclaims locked rent from Kora-sponsored accounts. Full architecture with safety checks, SQLite tracking, and Telegram alerts.</p>
      <span class="service-tag">Solana / TypeScript</span>
      <div style="margin-top: 0.75rem;"><a href="https://github.com/Usman3801/kora-rent-reclaim-bot" style="color: var(--accent); text-decoration: none; font-size: 0.85rem;">View on GitHub &rarr;</a></div>
    </div>
    <div class="service-card">
      <div class="service-icon" style="background: rgba(0,229,160,0.1); color: var(--accent2);">O</div>
      <h3>GenLayer Oracles</h3>
      <p>Secure oracle library for GenLayer Intelligent Contracts — crypto prices, weather, sports, forex with multi-source validation, manipulation detection, and TWAP support.</p>
      <span class="service-tag">Python / Web3</span>
      <div style="margin-top: 0.75rem;"><a href="https://github.com/Usman3801/genlayer-oracles" style="color: var(--accent); text-decoration: none; font-size: 0.85rem;">View on GitHub &rarr;</a></div>
    </div>
    <div class="service-card">
      <div class="service-icon" style="background: rgba(200,255,0,0.1); color: var(--accent);">&lt;/&gt;</div>
      <h3>Frontend Mentor Challenges</h3>
      <p>Social links profile, recipe page, bento grid, and more — clean responsive builds using HTML, CSS, and Bootstrap. Building my frontend fundamentals one challenge at a time.</p>
      <span class="service-tag">HTML / CSS / JS</span>
      <div style="margin-top: 0.75rem;"><a href="https://github.com/Usman3801" style="color: var(--accent); text-decoration: none; font-size: 0.85rem;">View all repos &rarr;</a></div>
    </div>
    <div class="service-card">
      <div class="service-icon" style="background: rgba(255,120,80,0.1); color: #ff7850;">JS</div>
      <h3>Interactive Web Apps</h3>
      <p>Drum Kit with keyboard events and audio, a Calculator with full state management, and TinDog — a Bootstrap landing page with animated gradients and responsive pricing.</p>
      <span class="service-tag">JavaScript / Bootstrap</span>
      <div style="margin-top: 0.75rem;"><a href="https://usman3801.github.io/tindog/" style="color: var(--accent); text-decoration: none; font-size: 0.85rem;">Live demo &rarr;</a></div>
    </div>
  </div>
</section>

<section>
  <div class="edge-section">
    <div class="section-label">The edge</div>
    <h2 class="section-title">Why "just use ChatGPT" doesn't replace me</h2>
    <p class="section-desc">AI is a power tool. I'm the person who knows which tool to pick, how to aim it, and how to deliver a finished product — not a rough draft.</p>
    <div class="edge-grid">
      <div class="edge-item">
        <div class="edge-num">01</div>
        <div>
          <h4>I ship, not just generate</h4>
          <p>You get deployed sites, published articles, and working automations. Not code dumps and "here's what I came up with."</p>
        </div>
      </div>
      <div class="edge-item">
        <div class="edge-num">02</div>
        <div>
          <h4>Strategy before execution</h4>
          <p>I figure out what you actually need before building. AI can't ask the right questions — I can.</p>
        </div>
      </div>
      <div class="edge-item">
        <div class="edge-num">03</div>
        <div>
          <h4>2x speed, half the cost</h4>
          <p>AI-augmented workflow means I deliver faster than traditional freelancers. You save time and money.</p>
        </div>
      </div>
      <div class="edge-item">
        <div class="edge-num">04</div>
        <div>
          <h4>Revisions included</h4>
          <p>I iterate until you're happy. Unlike raw AI output, my deliverables go through human review and quality control.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<section id="process">
  <div class="section-label">Process</div>
  <h2 class="section-title">How we work together</h2>
  <p class="section-desc">Simple, fast, no bloat. Most projects go from brief to delivery within a week.</p>

  <div class="process-steps">
    <div class="step">
      <h4>You tell me what you need</h4>
      <p>Quick call or message. I'll ask targeted questions to understand your goals, audience, and constraints.</p>
    </div>
    <div class="step">
      <h4>I scope & quote</h4>
      <p>You get a clear price, timeline, and deliverable list within 24 hours. No surprises.</p>
    </div>
    <div class="step">
      <h4>I build & deliver</h4>
      <p>Fast execution using AI-augmented workflows. You see progress, give feedback, and get revisions.</p>
    </div>
    <div class="step">
      <h4>You launch</h4>
      <p>Final delivery with deployment support, documentation, and a handoff you can actually use.</p>
    </div>
  </div>
</section>

<section id="pricing">
  <div class="section-label">Pricing</div>
  <h2 class="section-title">Transparent pricing</h2>
  <p class="section-desc">No hourly billing games. Fixed prices for defined deliverables.</p>

  <div class="pricing-grid">
    <div class="price-card">
      <div class="price-label">Blog post / article</div>
      <div class="price-amount">$30–80 <span>/ piece</span></div>
      <p class="price-desc">SEO-optimized, research-backed content ready to publish</p>
      <ul class="price-features">
        <li>1,000–3,000 words</li>
        <li>Keyword research included</li>
        <li>2 rounds of revisions</li>
        <li>48-hour turnaround</li>
      </ul>
    </div>
    <div class="price-card featured">
      <div class="price-label">Landing page</div>
      <div class="price-amount">$150–400 <span>/ page</span></div>
      <p class="price-desc">Designed, built, and deployed — fully responsive</p>
      <ul class="price-features">
        <li>Custom design & code</li>
        <li>Mobile-first responsive</li>
        <li>Deployment included</li>
        <li>5-7 day delivery</li>
      </ul>
    </div>
    <div class="price-card">
      <div class="price-label">AI workflow setup</div>
      <div class="price-amount">$100–300 <span>/ project</span></div>
      <p class="price-desc">Custom automation tailored to your business needs</p>
      <ul class="price-features">
        <li>Workflow audit & design</li>
        <li>Implementation & testing</li>
        <li>Documentation & training</li>
        <li>1 week of support</li>
      </ul>
    </div>
  </div>
</section>

<section class="cta-section" id="contact">
  <div class="section-label">Let's work</div>
  <h2>Got a project? <em style="color: var(--accent); font-style: normal;">Let's talk.</em></h2>
  <p>Send me a message with what you need. I'll get back to you within 24 hours with a scope and quote.</p>
  <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
    <a href="/cdn-cgi/l/email-protection#3857415d5a59555152514d4b55595659415755515c5d785f55595154165b5755" class="btn btn-primary">Email me &rarr;</a>
    <a href="https://x.com/maleficboss" class="btn btn-ghost">X / Twitter</a>
    <a href="https://github.com/Usman3801" class="btn btn-ghost">GitHub</a>
  </div>
  <p style="margin-top: 1.5rem; font-family: var(--mono); font-size: 0.8rem; color: var(--muted);">
    <a href="/cdn-cgi/l/email-protection" class="__cf_email__" data-cfemail="76190f1314171b1f1c1f03051b1718170f191b1f121336111b171f1a5815191b
